
#for perticular topic if we are creaing urls then we can make seperate file of topicwise urls
# from django.urls import URLPattern
# from django.urls import path
# from .views import EmployeeCreate, EmployeeDelete, EmployeeDetail, EmployeeUpdate, EmployeeRetrieve

# app_name = "book"
# urlpatterns = [

#     path('emp-gcreate/', EmployeeCreate.as_view(), name = 'EmployeeCreate'),
#     path('emp-retr/', EmployeeRetrieve.as_view(), name = 'EmployeeRetrieve'), 
#     path('emp-retr/<int:pk>/', EmployeeDetail.as_view(), name = 'EmployeeDetail'), 
#     path('<int:pk>/update/', EmployeeUpdate.as_view(), name = 'EmployeeUpdate'),  
#     path('<int:pk>/delete/', EmployeeDelete.as_view(), name = 'EmployeeDelete'),


# ]